# Copy to keystore.properties (gitignored) and fill real values.
# Keep expenso-upload.keystore + passwords backed up offline — losing them blocks Play uploads.
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=expenso-upload
storeFile=expenso-upload.keystore
